<nav class="fixed top-0 left-0 right-0 bg-white flex w-full justify-between p-4 shadow-md">
    <div class="space-x-3 flex items-center">
        <a href="/" class="text-2xl font-bold">only<span class="text-violet-500">FUN!</span></a>
    </div>
    <div class="space-x-3 flex items-center">
        <% if (currentUser) { %>
            <a href="/message/new" class="bg-violet-600 text-white font-semibold px-3 py-2 rounded-md hover:bg-violet-500 active:bg-violet-700 transition focus:outline-none focus:ring focus:ring-offset-2 focus:ring-violet-600">
                <i class="fas fa-plus-circle"></i>
                New message
            </a>
            <a href="<%= currentUser.url %>">
                <img src="<%= currentUser.avatar %>" alt="User avatar" class="w-10 h-10 rounded-full"/>
            </a>
        <% } else { %>
            <a href="/signup" class="font-semibold">Sign up</a>
            <a href="/login" class="bg-violet-600 text-white font-semibold px-3 py-2 rounded-md hover:bg-violet-500 active:bg-violet-700 transition focus:outline-none focus:ring focus:ring-offset-2 focus:ring-violet-600">
                <i class="fas fa-sign-in-alt"></i>
                Log in
            </a>
        <% } %>
    </div>
</nav>